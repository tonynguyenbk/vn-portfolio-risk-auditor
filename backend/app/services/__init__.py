"""Numerical services.

Nothing in this package may import SciPy. See the note in ``pyproject.toml``:
SciPy is a test-only dependency used as an independent oracle, and the runtime
relies on ``math`` and ``statistics`` from the standard library instead.
"""
