# Copy to .env.local for local development.
#
# Origin of the Python risk engine. Leave empty in production, where the API is
# served from the same origin as the site; set it when the backend lives
# somewhere else. Only the upload path uses it — the bundled demonstration is
# precomputed and never issues a request.
NEXT_PUBLIC_API_URL=http://localhost:8000
